language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-bro' ]; then cd .. && eval "mv $currentfolder generator-bro" && cd generator-bro; fi
